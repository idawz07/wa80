{
  "name": "wa80-guide-ios-wrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the WA80-8 field video guide.",
  "scripts": {
    "sync:web": "powershell -ExecutionPolicy Bypass -File .\\scripts\\sync-web-assets.ps1",
    "cap:copy": "npm run sync:web && npx cap copy",
    "cap:sync": "npm run sync:web && npx cap sync",
    "cap:add:ios": "npm run sync:web && npx cap add ios",
    "cap:open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "typescript": "^5.9.3"
  }
}
